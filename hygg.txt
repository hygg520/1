local WindUI = loadstring(game:HttpGet("https://hygg520.github.io/1/main.txt"))() local BRAND = {name = "hygg脚本", version = "v1.3.4", author = "hygg", folder = "hyggHub", icon = "zap", theme = "Dark", accent = "#FF6B35"} local Window = WindUI:CreateWindow({ Title = BRAND.name, Icon = BRAND.icon, IconThemed = true, Author = BRAND.version, Folder = BRAND.folder, Size = UDim2.fromOffset(640, 420), Transparent = true, Theme = BRAND.theme, HideSearchBar = true, Resizable = true, SideBarWidth = 240, Background = "rbxassetid://84674071500810", BackgroundImageTransparency = 0.5, User = {Enabled = true, Callback = function() WindUI:Notify({Title = "玩家信息", Content = game.Players.LocalPlayer.Name, Duration = 2, Icon = "user"}) end}, }) Window:EditOpenButton({ Title = "hygg", Icon = "", CornerRadius = UDim.new(0, 26), StrokeThickness = 1.5, Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 0)), ColorSequenceKeypoint.new(0.16, Color3.fromRGB(255, 255, 0)), ColorSequenceKeypoint.new(0.33, Color3.fromRGB(0, 255, 0)), ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 255, 255)), ColorSequenceKeypoint.new(0.66, Color3.fromRGB(0, 0, 255)), ColorSequenceKeypoint.new(0.83, Color3.fromRGB(255, 0, 255)), ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 0)) }), Draggable = true, }) task.spawn(function() task.wait(0.6) pcall(function() local openBtn = Window.OpenButton if not openBtn and Window.Instance then openBtn = Window.Instance:FindFirstChild("OpenButton", true) or Window.Instance:FindFirstChildWhichIsA("TextButton", true) end if openBtn then local stroke = openBtn:FindFirstChildOfClass("UIStroke") local gradient = openBtn:FindFirstChildOfClass("UIGradient") if stroke and gradient then local currentAngle = 0 local connection connection = game:GetService("RunService").RenderStepped:Connect(function(dt) if stroke and stroke.Parent then currentAngle = (currentAngle + dt * 150) % 360 gradient.Rotation = currentAngle else connection:Disconnect() end end) end end end) end) task.spawn(function() task.wait(0.5) pcall(function() local mainFrame = Window.Frame or Window.Main or (Window.UIElements and Window.UIElements.Main) if not mainFrame and Window.Instance then mainFrame = Window.Instance:FindFirstChildWhichIsA("Frame", true) end if mainFrame then local Stroke = Instance.new("UIStroke") Stroke.Name = "RainbowBorder" Stroke.Thickness = 3.5 Stroke.Color = Color3.new(1, 1, 1) Stroke.LineJoinMode = Enum.LineJoinMode.Round Stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border Stroke.Parent = mainFrame local Gradient = Instance.new("UIGradient") Gradient.Name = "RainbowGradient" Gradient.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 0)), ColorSequenceKeypoint.new(0.16, Color3.fromRGB(255, 255, 0)), ColorSequenceKeypoint.new(0.33, Color3.fromRGB(0, 255, 0)), ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 255, 255)), ColorSequenceKeypoint.new(0.66, Color3.fromRGB(0, 0, 255)), ColorSequenceKeypoint.new(0.83, Color3.fromRGB(255, 0, 255)), ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 0)) }) Gradient.Parent = Stroke local Corner = mainFrame:FindFirstChildOfClass("UICorner") if not Corner then Corner = Instance.new("UICorner") Corner.CornerRadius = UDim.new(0, 12) Corner.Parent = mainFrame end local currentAngle = 0 local mainConnection mainConnection = game:GetService("RunService").RenderStepped:Connect(function(dt) if Stroke and Stroke.Parent then currentAngle = (currentAngle + dt * 150) % 360 Gradient.Rotation = currentAngle else mainConnection:Disconnect() end end) local topbar = mainFrame:FindFirstChild("Topbar") or mainFrame:FindFirstChild("Header") or mainFrame local titleObj = nil for _, child in ipairs(topbar:GetDescendants()) do if child:IsA("TextLabel") and (child.Text == BRAND.name or child.Name:lower():find("title")) then titleObj = child break end end local timeLabel = Instance.new("TextLabel") timeLabel.Name = "CustomTimeLabel" timeLabel.Parent = topbar timeLabel.BackgroundTransparency = 1 timeLabel.Size = UDim2.new(0, 100, 0, 30) timeLabel.Position = UDim2.new(0, 150, 0, 8) timeLabel.Font = Enum.Font.GothamBold if titleObj then timeLabel.TextColor3 = titleObj.TextColor3 else timeLabel.TextColor3 = Color3.fromHex(BRAND.accent) end timeLabel.TextSize = 14 timeLabel.TextXAlignment = Enum.TextXAlignment.Left timeLabel.ZIndex = 100 task.spawn(function() while timeLabel and timeLabel.Parent do timeLabel.Text = os.date("%H:%M:%S") if titleObj then timeLabel.TextColor3 = titleObj.TextColor3 end task.wait(1) end end) end end) end) local function safeLoadModule(url, moduleName) local success, result = pcall(function() local code = game:HttpGet(url) local fn = loadstring(code) if not fn then error("loadstring 返回 nil") end return fn() end) if success and result then local execSuccess, execErr = pcall(result, Window, WindUI) if not execSuccess then WindUI:Notify({Title = moduleName .. "模块执行失败", Content = "模块执行出错", Duration = 3, Icon = "alert"}) return false end return true else WindUI:Notify({Title = moduleName .. "模块加载失败", Content = "请检查网络或仓库地址", Duration = 3, Icon = "alert"}) return false end end safeLoadModule("https://hygg520.github.io/1/通用.txt", "通用") safeLoadModule("https://hygg520.github.io/1/传送.txt", "传送") safeLoadModule("https://hygg520.github.io/1/玩家.txt", "玩家") safeLoadModule("https://hygg520.github.io/1/黑洞.txt", "黑洞") safeLoadModule("https://hygg520.github.io/1/动作.txt", "动作") safeLoadModule("https://raw.githubusercontent.com/hygg520/1/refs/heads/main/服务器功能.txt", "服务器功能") _G._LCF_WindUI = WindUI _G._LCF_TmplWin = Window
