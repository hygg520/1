return function(Window, WindUI) local Players = game:GetService("Players") local RunService = game:GetService("RunService") local UserInputService = game:GetService("UserInputService") local VirtualUser = game:GetService("VirtualUser") local VirtualInput = game:GetService("VirtualInputManager") local Lighting = game:GetService("Lighting") local Workspace = game:GetService("Workspace") local TweenService = game:GetService("TweenService") local ReplicatedStorage = game:GetService("ReplicatedStorage") local LocalPlayer = Players.LocalPlayer local tab = Window:Tab({Title = "通用", Icon = "settings"}) local suggestSection = tab:Section({Title = "建议开"}) local function createSuggestToggle(title, callback) return suggestSection:Toggle({Title = title, Value = false, Callback = callback}) end createSuggestToggle("防踢", function(val) if val then local con = LocalPlayer.Idled:Connect(function() VirtualUser:CaptureController() VirtualUser:ClickButton2(Vector2.new()) end) _G.AntiAFKCon = con else if _G.AntiAFKCon then _G.AntiAFKCon:Disconnect() _G.AntiAFKCon = nil end end end) local function createNoFall() local con, charCon = nil, nil local z = Vector3.zero local function bind(c) if not c then return end local r = c:WaitForChild("HumanoidRootPart", 5) if r then if con then con:Disconnect() con = nil end con = RunService.Heartbeat:Connect(function() if not r.Parent then if con then con:Disconnect() con = nil end return end local v = r.AssemblyLinearVelocity r.AssemblyLinearVelocity = z RunService.RenderStepped:Wait() r.AssemblyLinearVelocity = v end) end end return { start = function() if charCon then charCon:Disconnect() end bind(LocalPlayer.Character) charCon = LocalPlayer.CharacterAdded:Connect(bind) end, stop = function() if con then con:Disconnect() end if charCon then charCon:Disconnect() charCon = nil end end } end local noFallApi = nil createSuggestToggle("防摔", function(val) if not noFallApi then noFallApi = createNoFall() end if val then noFallApi.start() else noFallApi.stop() end end) local originalLighting = {} local originalAtmosphere = {} createSuggestToggle("去雾", function(enable) if enable then originalLighting.FogStart = Lighting.FogStart originalLighting.FogEnd = Lighting.FogEnd originalLighting.FogColor = Lighting.FogColor Lighting.FogStart = 0 Lighting.FogEnd = math.huge Lighting.FogColor = Color3.fromRGB(200, 200, 220) for _, obj in pairs(Lighting:GetChildren()) do if obj:IsA("Atmosphere") then if not originalAtmosphere[obj] then originalAtmosphere[obj] = {Density = obj.Density, Offset = obj.Offset, Glare = obj.Glare, Haze = obj.Haze} end obj.Density = 0 obj.Offset = 0 obj.Glare = 0 obj.Haze = 0 end end else if originalLighting.FogStart then Lighting.FogStart = originalLighting.FogStart end if originalLighting.FogEnd then Lighting.FogEnd = originalLighting.FogEnd end if originalLighting.FogColor then Lighting.FogColor = originalLighting.FogColor end for obj, props in pairs(originalAtmosphere) do if obj and obj.Parent then obj.Density = props.Density obj.Offset = props.Offset obj.Glare = props.Glare obj.Haze = props.Haze end end originalAtmosphere = {} end end) local antiPushConn = nil createSuggestToggle("防甩飞", function(val) if val then if antiPushConn then antiPushConn:Disconnect() end antiPushConn = RunService.Stepped:Connect(function() local char = LocalPlayer.Character if char then local root = char:FindFirstChild("HumanoidRootPart") if root then root.CanCollide = false end end end) else if antiPushConn then antiPushConn:Disconnect() antiPushConn = nil end local char = LocalPlayer.Character if char then local root = char:FindFirstChild("HumanoidRootPart") if root then root.CanCollide = true end end end end) local antiPullbackConn = nil local antiPullbackSpeed = 100 createSuggestToggle("防回拉", function(val) if val then if antiPullbackConn then antiPullbackConn:Disconnect() end antiPullbackConn = RunService.Heartbeat:Connect(function() local char = LocalPlayer.Character if not char then return end local hum = char:FindFirstChildOfClass("Humanoid") local root = char:FindFirstChild("HumanoidRootPart") if not hum or not root then return end if hum.MoveDirection.Magnitude > 0 then char:TranslateBy(hum.MoveDirection * antiPullbackSpeed / 20) end end) else if antiPullbackConn then antiPullbackConn:Disconnect() antiPullbackConn = nil end end end) createSuggestToggle("无限跳", function(val) if val then local conn = UserInputService.JumpRequest:Connect(function() if LocalPlayer and LocalPlayer.Character then local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid") if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end end end) _G.InfJumpCon = conn else if _G.InfJumpCon then _G.InfJumpCon:Disconnect() _G.InfJumpCon = nil end end end) local instantRespawnEnabled = false local instantRespawnLoop = nil createSuggestToggle("秒复活", function(val) instantRespawnEnabled = val if val then if instantRespawnLoop then task.cancel(instantRespawnLoop) end instantRespawnLoop = task.spawn(function() while instantRespawnEnabled do local character = LocalPlayer.Character if character then local humanoid = character:FindFirstChildOfClass("Humanoid") if humanoid and humanoid.Health <= 0 then LocalPlayer:LoadCharacter() task.wait(0.5) end end task.wait(0.1) end end) else if instantRespawnLoop then task.cancel(instantRespawnLoop) instantRespawnLoop = nil end end end) local instantBuyEnabled = false local instantBuyAddedConnection = nil local function setupInstantBuy() for _, v in pairs(workspace:GetDescendants()) do if v:IsA("ProximityPrompt") then v.HoldDuration = 0 end end end createSuggestToggle("瞬间交互", function(val) instantBuyEnabled = val if val then setupInstantBuy() if instantBuyAddedConnection then instantBuyAddedConnection:Disconnect() end instantBuyAddedConnection = workspace.DescendantAdded:Connect(function(v) if v:IsA("ProximityPrompt") and instantBuyEnabled then v.HoldDuration = 0 end end) else if instantBuyAddedConnection then instantBuyAddedConnection:Disconnect() instantBuyAddedConnection = nil end end end) local unbreakLimbsEnabled = false local unbreakLimbsConn = nil createSuggestToggle("肢体不碎", function(val) unbreakLimbsEnabled = val if val then local function setup() if type(CharStats) == "function" and me then local limbsfolder = CharStats(me).HealthValues if not limbsfolder then return end for _, a in pairs(limbsfolder:GetChildren()) do for _, i in pairs(a:GetChildren()) do if i and i.Name == "Broken" then i.Value = false if not unbreakLimbsConn then unbreakLimbsConn = i:GetPropertyChangedSignal("Value"):Connect(function() if unbreakLimbsEnabled then i.Value = false end end) end end end end end end setup() else if unbreakLimbsConn then unbreakLimbsConn:Disconnect() unbreakLimbsConn = nil end end end) local teleportToolEnabled = false local teleportToolConnection = nil local teleportTool = nil local function createTeleportTool() if teleportTool then return end local Mouse = LocalPlayer:GetMouse() local Camera = workspace.CurrentCamera local Tool = Instance.new("Tool") Tool.Name = "点击传送" Tool.RequiresHandle = false Tool.Activated:Connect(function() local Character = LocalPlayer.Character if not Character then return end local HRP = Character:FindFirstChild("HumanoidRootPart") if not HRP then return end local params = RaycastParams.new() params.FilterDescendantsInstances = {Character} params.FilterType = Enum.RaycastFilterType.Blacklist local unitRay = Camera:ScreenPointToRay(Mouse.X, Mouse.Y) local result = workspace:Raycast(unitRay.Origin, unitRay.Direction * 100000, params) if not result then return end HRP.CFrame = CFrame.new(result.Position + Vector3.new(0, 3, 0)) end) teleportTool = Tool local function equipTool() if teleportToolEnabled and teleportTool then local backpack = LocalPlayer:FindFirstChild("Backpack") if backpack and teleportTool.Parent ~= backpack then teleportTool.Parent = backpack end end end equipTool() if teleportToolConnection then teleportToolConnection:Disconnect() end teleportToolConnection = LocalPlayer.CharacterAdded:Connect(function() task.wait(0.5) equipTool() end) end local function destroyTeleportTool() if teleportToolConnection then teleportToolConnection:Disconnect() teleportToolConnection = nil end if teleportTool then teleportTool:Destroy() teleportTool = nil end end createSuggestToggle("点击传送工具", function(val) teleportToolEnabled = val if val then createTeleportTool() else destroyTeleportTool() end end) local groupBypassHook = nil createSuggestToggle("群组验证绕过", function(val) if val then if not groupBypassHook then local OldNameCall OldNameCall = hookmetamethod(game, "__namecall", function(self, ...) if self ~= LocalPlayer or getnamecallmethod() ~= "IsInGroup" then return OldNameCall(self, ...) end return true end) hookfunction(LocalPlayer.IsInGroup, function(self, ...) return true end) groupBypassHook = {OldNameCall = OldNameCall} end end end) createSuggestToggle("一键清屏", function(val) if val then local CoreGui = game:GetService("CoreGui") local PlayerGui = LocalPlayer:WaitForChild("PlayerGui") if PlayerGui:FindFirstChild("FullUIToggle") then PlayerGui.FullUIToggle:Destroy() end local Hidden = false local Stored = {} local ScreenGui = Instance.new("ScreenGui") ScreenGui.Name = "FullUIToggle" ScreenGui.ResetOnSpawn = false ScreenGui.Parent = PlayerGui local Button = Instance.new("TextButton") Button.Size = UDim2.new(0, 120, 0, 30) Button.Position = UDim2.new(1, -130, 0, 10) Button.BackgroundColor3 = Color3.fromRGB(20, 20, 20) Button.TextColor3 = Color3.new(1, 1, 1) Button.Text = "隐藏" Button.Parent = ScreenGui Instance.new("UICorner", Button) local function IsUI(obj) return obj:IsA("GuiObject") end local function ShouldSkip(obj) return obj:IsDescendantOf(ScreenGui) end local function Process(container, hide) for _, obj in ipairs(container:GetDescendants()) do if IsUI(obj) and not ShouldSkip(obj) then if hide then if not Stored[obj] then Stored[obj] = obj.Visible end obj.Visible = false else if Stored[obj] ~= nil then obj.Visible = Stored[obj] end end end end end local function ToggleUI(state) if state then Stored = {} Process(PlayerGui, true) Process(CoreGui, true) else Process(PlayerGui, false) Process(CoreGui, false) Stored = {} end end Button.MouseButton1Click:Connect(function() Hidden = not Hidden ToggleUI(Hidden) Button.Text = Hidden and "显示" or "隐藏" end) local function Hook(container) container.DescendantAdded:Connect(function(obj) if Hidden and obj:IsA("GuiObject") and not ShouldSkip(obj) then Stored[obj] = obj.Visible obj.Visible = false end end) end Hook(PlayerGui) Hook(CoreGui) else local PlayerGui = LocalPlayer:FindFirstChild("PlayerGui") if PlayerGui and PlayerGui:FindFirstChild("FullUIToggle") then PlayerGui.FullUIToggle:Destroy() end end end) tab:Divider() local independentSection = tab:Section({Title = "独立功能"}) local speedEnabled = false local speedSliderValue = 1 local speedCFrameConn = nil local speedWalkSpeedMonitor = nil local gameDefaultWalkSpeed = 16 local function monitorWalkSpeed() if speedWalkSpeedMonitor then speedWalkSpeedMonitor:Disconnect() speedWalkSpeedMonitor = nil end local char = LocalPlayer.Character local hum = char and char:FindFirstChildOfClass("Humanoid") if not hum then return end speedWalkSpeedMonitor = hum:GetPropertyChangedSignal("WalkSpeed"):Connect(function() if not speedEnabled then gameDefaultWalkSpeed = hum.WalkSpeed end end) gameDefaultWalkSpeed = hum.WalkSpeed end LocalPlayer.CharacterAdded:Connect(function() task.wait(0.5) if not speedEnabled then monitorWalkSpeed() end end) independentSection:Toggle({ Title = "奔跑速度开关", Value = false, Callback = function(val) speedEnabled = val if val then if speedCFrameConn then speedCFrameConn:Disconnect() end local char = LocalPlayer.Character local hum = char and char:FindFirstChildOfClass("Humanoid") if hum then gameDefaultWalkSpeed = hum.WalkSpeed end if speedWalkSpeedMonitor then speedWalkSpeedMonitor:Disconnect() speedWalkSpeedMonitor = nil end speedCFrameConn = RunService.RenderStepped:Connect(function() local char = LocalPlayer.Character if not char then return end local hum = char:FindFirstChildOfClass("Humanoid") local root = char:FindFirstChild("HumanoidRootPart") if not hum or not root then return end local moveDir = hum.MoveDirection if moveDir.Magnitude > 0 then local actualSpeed = speedSliderValue * 32 root.CFrame = root.CFrame + moveDir * (actualSpeed * 0.05) end end) else if speedCFrameConn then speedCFrameConn:Disconnect() speedCFrameConn = nil end local char = LocalPlayer.Character local hum = char and char:FindFirstChildOfClass("Humanoid") if hum then hum.WalkSpeed = gameDefaultWalkSpeed end monitorWalkSpeed() end end }) independentSection:Slider({ Title = "奔跑速度", Value = {Min = 1, Max = 40, Default = 1}, Callback = function(value) speedSliderValue = math.floor(value) end }) local jumpEnabled = false local jumpSliderValue = 1 local jumpLockConnection = nil local gameDefaultJumpPower = 50 local jumpPowerMonitor = nil local function monitorJumpPower() if jumpPowerMonitor then jumpPowerMonitor:Disconnect() jumpPowerMonitor = nil end local char = LocalPlayer.Character local hum = char and char:FindFirstChildOfClass("Humanoid") if not hum then return end jumpPowerMonitor = hum:GetPropertyChangedSignal("JumpPower"):Connect(function() if not jumpEnabled then gameDefaultJumpPower = hum.JumpPower end end) gameDefaultJumpPower = hum.JumpPower end local function stopJumpLock() if jumpLockConnection then jumpLockConnection:Disconnect() jumpLockConnection = nil end end local function startJumpLock(value) stopJumpLock() if not jumpEnabled then return end local actualJumpPower = value * 50 jumpLockConnection = RunService.Heartbeat:Connect(function() local char = LocalPlayer.Character if char then local hum = char:FindFirstChildOfClass("Humanoid") if hum then hum.UseJumpPower = true if hum.JumpPower ~= actualJumpPower then hum.JumpPower = actualJumpPower end end end end) end LocalPlayer.CharacterAdded:Connect(function() task.wait(0.5) if jumpEnabled then startJumpLock(jumpSliderValue) else monitorJumpPower() end end) independentSection:Toggle({ Title = "跳跃高度开关", Value = false, Callback = function(val) jumpEnabled = val if val then local char = LocalPlayer.Character local hum = char and char:FindFirstChildOfClass("Humanoid") if hum then gameDefaultJumpPower = hum.JumpPower end if jumpPowerMonitor then jumpPowerMonitor:Disconnect() jumpPowerMonitor = nil end startJumpLock(jumpSliderValue) else stopJumpLock() local char = LocalPlayer.Character local hum = char and char:FindFirstChildOfClass("Humanoid") if hum then hum.JumpPower = gameDefaultJumpPower end monitorJumpPower() end end }) independentSection:Slider({ Title = "跳跃高度", Value = {Min = 1, Max = 60, Default = 1}, Callback = function(value) jumpSliderValue = math.floor(value) if jumpEnabled then startJumpLock(jumpSliderValue) end end }) local gravityEnabled = false local gravitySliderValue = 1 local gravityHeartbeat = nil local gameDefaultGravity = 196.2 local gravityMonitor = nil local function monitorGravity() if gravityMonitor then gravityMonitor:Disconnect() gravityMonitor = nil end gravityMonitor = RunService.Stepped:Connect(function() if not gravityEnabled then gameDefaultGravity = workspace.Gravity end end) gameDefaultGravity = workspace.Gravity end local function startGravityLock(ratio) if gravityHeartbeat then gravityHeartbeat:Disconnect() gravityHeartbeat = nil end if not gravityEnabled then return end local targetGravity = ratio * 196.2 gravityHeartbeat = RunService.Heartbeat:Connect(function() workspace.Gravity = targetGravity end) end independentSection:Toggle({ Title = "重力开关", Value = false, Callback = function(val) gravityEnabled = val if val then gameDefaultGravity = workspace.Gravity if gravityMonitor then gravityMonitor:Disconnect() gravityMonitor = nil end startGravityLock(gravitySliderValue) else if gravityHeartbeat then gravityHeartbeat:Disconnect() gravityHeartbeat = nil end workspace.Gravity = gameDefaultGravity monitorGravity() end end }) independentSection:Slider({ Title = "重力", Value = {Min = 1, Max = 40, Default = 1}, Callback = function(value) gravitySliderValue = math.floor(value) if gravityEnabled then startGravityLock(gravitySliderValue) end end }) independentSection:Toggle({ Title = "穿墙", Value = false, Callback = function(val) if val then _G.NoclipCon = RunService.Stepped:Connect(function() local char = LocalPlayer.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") if not root then return end root.CanCollide = false for _, v in ipairs(char:GetDescendants()) do if v:IsA("BasePart") and v.CanCollide then v.CanCollide = false end end end) else if _G.NoclipCon then _G.NoclipCon:Disconnect() _G.NoclipCon = nil end local char = LocalPlayer.Character if char then local root = char:FindFirstChild("HumanoidRootPart") if root then root.CanCollide = true end for _, v in ipairs(char:GetDescendants()) do if v:IsA("BasePart") and not v.CanCollide then v.CanCollide = true end end end end end }) local superNoclipState = false local tpWalkConn = nil local noclipConn = nil local tpWalkSpeed = 1 independentSection:Toggle({ Title = "穿墙2.0", Value = false, Callback = function(val) superNoclipState = val if val then if not tpWalkConn then tpWalkConn = RunService.RenderStepped:Connect(function(delta) local char = LocalPlayer.Character if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChild("Humanoid") then local hum = char.Humanoid if hum.MoveDirection.Magnitude > 0 then char.HumanoidRootPart.CFrame = char.HumanoidRootPart.CFrame + hum.MoveDirection * (tpWalkSpeed * delta * 40) end end end) end if not noclipConn then noclipConn = RunService.Stepped:Connect(function() if LocalPlayer.Character then for _, part in ipairs(LocalPlayer.Character:GetDescendants()) do if part:IsA("BasePart") then part.CanCollide = false end end end end) end else if tpWalkConn then tpWalkConn:Disconnect() tpWalkConn = nil end if noclipConn then noclipConn:Disconnect() noclipConn = nil end local char = LocalPlayer.Character if char then for _, part in ipairs(char:GetDescendants()) do if part:IsA("BasePart") then part.CanCollide = true end end end end end }) independentSection:Slider({ Title = "穿墙行走速度", Value = {Min = 1, Max = 50, Default = 1}, Callback = function(value) tpWalkSpeed = math.floor(value) end }) independentSection:Toggle({ Title = "锁定视角", Value = false, Callback = function(val) if val then _G.LockViewCon = RunService:BindToRenderStep("LockView", Enum.RenderPriority.Camera.Value + 1, function() local char = LocalPlayer.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") if not root then return end local camDir = workspace.CurrentCamera.CFrame.LookVector local flatDir = Vector3.new(camDir.X, 0, camDir.Z) if flatDir.Magnitude > 0.01 then root.CFrame = CFrame.new(root.Position, root.Position + flatDir) end end) else if _G.LockViewCon then RunService:UnbindFromRenderStep("LockView") _G.LockViewCon = nil end end end }) independentSection:Toggle({ Title = "飞行", Value = false, Callback = function(val) local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() local hum = char:FindFirstChildOfClass("Humanoid") local root = char:FindFirstChild("HumanoidRootPart") if val then if hum then for _, state in pairs(Enum.HumanoidStateType:GetEnumItems()) do hum:SetStateEnabled(state, false) end hum:ChangeState(Enum.HumanoidStateType.Swimming) end if char.Animate then char.Animate.Disabled = true end local bg = Instance.new("BodyGyro", root) bg.P = 9e4 bg.maxTorque = Vector3.new(9e9, 9e9, 9e9) bg.CFrame = root.CFrame local bv = Instance.new("BodyVelocity", root) bv.Velocity = Vector3.new(0, 0, 0) bv.MaxForce = Vector3.new(9e9, 9e9, 9e9) if hum then hum.PlatformStand = true end _G.FlyBody = {bg = bg, bv = bv} _G.FlyEnabled = true _G.FlyCon1 = RunService.RenderStepped:Connect(function() if not _G.FlyEnabled or not char or not hum or hum.Health <= 0 then if _G.FlyCon1 then _G.FlyCon1:Disconnect() end return end if root then bg.CFrame = workspace.CurrentCamera.CoordinateFrame end end) _G.FlyCon2 = RunService.Heartbeat:Connect(function() if not _G.FlyEnabled or not char or not hum then return end if hum.MoveDirection.Magnitude > 0 then char:TranslateBy(hum.MoveDirection * 1) end end) else if hum then for _, state in pairs(Enum.HumanoidStateType:GetEnumItems()) do hum:SetStateEnabled(state, true) end hum:ChangeState(Enum.HumanoidStateType.Running) hum.PlatformStand = false end if char.Animate then char.Animate.Disabled = false end if _G.FlyBody and _G.FlyBody.bg then _G.FlyBody.bg:Destroy() end if _G.FlyBody and _G.FlyBody.bv then _G.FlyBody.bv:Destroy() end _G.FlyBody = {} _G.FlyEnabled = false if _G.FlyCon1 then _G.FlyCon1:Disconnect() _G.FlyCon1 = nil end if _G.FlyCon2 then _G.FlyCon2:Disconnect() _G.FlyCon2 = nil end end end }) local function setTransparency(character, transparency) for _, part in pairs(character:GetDescendants()) do if part:IsA("BasePart") or part:IsA("Decal") then part.Transparency = transparency end end end independentSection:Toggle({ Title = "隐身", Value = false, Callback = function(val) if val then local char = LocalPlayer.Character if char and char:FindFirstChild("HumanoidRootPart") then local savedpos = char.HumanoidRootPart.CFrame task.wait() char:MoveTo(Vector3.new(-25.95, 84, 3537.55)) task.wait(0.15) local Seat = Instance.new('Seat', workspace) Seat.Anchored = false Seat.CanCollide = false Seat.Name = 'invischair' Seat.Transparency = 1 Seat.Position = Vector3.new(-25.95, 84, 3537.55) local Weld = Instance.new("Weld", Seat) Weld.Part0 = Seat Weld.Part1 = char:FindFirstChild("Torso") or char:FindFirstChild("UpperTorso") task.wait() Seat.CFrame = savedpos setTransparency(char, 0.5) end else local invisChair = workspace:FindFirstChild('invischair') if invisChair then invisChair:Destroy() end if LocalPlayer.Character then setTransparency(LocalPlayer.Character, 0) end end end }) independentSection:Toggle({ Title = "偷取所有物品", Value = false, Callback = function(val) _G.StealLoop = val if val then task.spawn(function() while _G.StealLoop do local myBackpack = LocalPlayer:FindFirstChild("Backpack") if not myBackpack then myBackpack = Instance.new("Backpack") myBackpack.Parent = LocalPlayer end for _, targetPlayer in ipairs(Players:GetPlayers()) do if targetPlayer == LocalPlayer or not targetPlayer.Character then continue end local containers = { targetPlayer:FindFirstChild("Backpack"), targetPlayer:FindFirstChild("Inventory"), targetPlayer:FindFirstChild("Storage"), targetPlayer:FindFirstChild("Bag"), targetPlayer.Character:FindFirstChild("Backpack") } for _, container in ipairs(containers) do if not container then continue end for _, item in ipairs(container:GetChildren()) do if item:IsA("Tool") or item:IsA("Model") or item:IsA("Part") or item:IsA("Accessory") then item.Parent = myBackpack task.wait(0.05) end end end end task.wait(1) end end) end end }) independentSection:Toggle({ Title = "防坐下", Value = false, Callback = function(val) local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() local humanoid = character:WaitForChild("Humanoid") if val then humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false) humanoid.Sit = false else humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true) humanoid.Sit = false end end }) local function placeOneBlock() local char = LocalPlayer.Character local root = char and char:FindFirstChild("HumanoidRootPart") if not char or not root then return end local block = Instance.new("Part") block.Name = "LocalBlock" block.Size = Vector3.new(6, 1, 6) block.Position = root.Position + Vector3.new(0, -3, 0) block.Anchored = true block.CanCollide = true block.BrickColor = BrickColor.new("White") block.Parent = workspace end independentSection:Toggle({ Title = "放置方块", Value = false, Callback = function(val) if val then if _G.BlockGui then _G.BlockGui:Destroy() end local sg = Instance.new("ScreenGui") sg.Name = "BlockPlaceGui" sg.Parent = LocalPlayer.PlayerGui sg.ResetOnSpawn = false _G.BlockGui = sg local mainFrame = Instance.new("Frame") mainFrame.Parent = sg mainFrame.Size = UDim2.new(0, 160, 0, 100) mainFrame.Position = UDim2.new(0.1, 0, 0.18, 0) mainFrame.BackgroundTransparency = 1 mainFrame.ZIndex = 9999 local btnClear = Instance.new("TextButton") btnClear.Parent = mainFrame btnClear.Size = UDim2.new(0, 50, 0, 50) btnClear.BackgroundColor3 = Color3.new(1, 0, 0) btnClear.TextColor3 = Color3.new(1, 1, 1) btnClear.Text = "清除" btnClear.Font = Enum.Font.GothamBold btnClear.TextSize = 14 btnClear.ZIndex = 10000 Instance.new("UICorner", btnClear).CornerRadius = UDim.new(0, 12) btnClear.MouseButton1Click:Connect(function() for _, v in pairs(workspace:GetChildren()) do if v.Name == "LocalBlock" then v:Destroy() end end end) local closeBtn = Instance.new("TextButton") closeBtn.Parent = mainFrame closeBtn.Size = UDim2.new(0, 50, 0, 50) closeBtn.Position = UDim2.new(0, 55, 0, 0) closeBtn.BackgroundColor3 = Color3.new(0, 0, 0) closeBtn.Text = "X" closeBtn.TextColor3 = Color3.new(1, 1, 1) closeBtn.TextSize = 18 closeBtn.ZIndex = 10002 Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0, 8) closeBtn.MouseButton1Click:Connect(function() _G.AutoPlace = false if _G.BlockGui then _G.BlockGui:Destroy() _G.BlockGui = nil end end) local autoBtn = Instance.new("TextButton") autoBtn.Parent = mainFrame autoBtn.Size = UDim2.new(0, 160, 0, 40) autoBtn.Position = UDim2.new(0, 0, 0, 55) autoBtn.BackgroundColor3 = Color3.new(0, 0, 0) autoBtn.Text = "自动放置" autoBtn.TextColor3 = Color3.new(1, 1, 1) autoBtn.Font = Enum.Font.GothamBold autoBtn.TextSize = 14 autoBtn.ZIndex = 10000 Instance.new("UICorner", autoBtn).CornerRadius = UDim.new(0, 12) autoBtn.MouseButton1Click:Connect(function() _G.AutoPlace = not _G.AutoPlace if _G.AutoPlace then autoBtn.BackgroundColor3 = Color3.new(0.2, 0.8, 0.2) task.spawn(function() while _G.AutoPlace do placeOneBlock() task.wait(0.1) end end) else autoBtn.BackgroundColor3 = Color3.new(0, 0, 0) end end) if makeDraggable then makeDraggable(mainFrame, mainFrame) end else if _G.BlockGui then _G.BlockGui:Destroy() _G.BlockGui = nil end _G.AutoPlace = false end end }) local respawnProtection = { _enabled = false, _lastPosition = nil, _deathConnection = nil, _charAddedConn = nil, } independentSection:Toggle({ Title = "原地复活", Value = false, Callback = function(val) respawnProtection._enabled = val if val then if respawnProtection._charAddedConn then respawnProtection._charAddedConn:Disconnect() end respawnProtection._charAddedConn = LocalPlayer.CharacterAdded:Connect(function(character) local humanoid = character:WaitForChild("Humanoid") if respawnProtection._deathConnection then respawnProtection._deathConnection:Disconnect() end respawnProtection._deathConnection = humanoid.Died:Connect(function() if respawnProtection._enabled and character:FindFirstChild("HumanoidRootPart") then respawnProtection._lastPosition = character.HumanoidRootPart.Position end end) if respawnProtection._enabled and respawnProtection._lastPosition then local hrp = character:WaitForChild("HumanoidRootPart") if hrp then hrp.CFrame = CFrame.new(respawnProtection._lastPosition) end end end) local char = LocalPlayer.Character if char then local humanoid = char:FindFirstChildOfClass("Humanoid") if humanoid then if respawnProtection._deathConnection then respawnProtection._deathConnection:Disconnect() end respawnProtection._deathConnection = humanoid.Died:Connect(function() if respawnProtection._enabled and char:FindFirstChild("HumanoidRootPart") then respawnProtection._lastPosition = char.HumanoidRootPart.Position end end) end end else if respawnProtection._deathConnection then respawnProtection._deathConnection:Disconnect() respawnProtection._deathConnection = nil end if respawnProtection._charAddedConn then respawnProtection._charAddedConn:Disconnect() respawnProtection._charAddedConn = nil end respawnProtection._lastPosition = nil end end }) local waterWalkConn = nil independentSection:Toggle({ Title = "水上行走", Value = false, Callback = function(val) if val then if waterWalkConn then waterWalkConn:Disconnect() end waterWalkConn = RunService.Heartbeat:Connect(function() local character = LocalPlayer.Character if character and character:FindFirstChild("Humanoid") then character.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Swimming, false) end end) else if waterWalkConn then waterWalkConn:Disconnect() waterWalkConn = nil end local character = LocalPlayer.Character if character and character:FindFirstChild("Humanoid") then character.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Swimming, true) end end end }) local carFlyEnabled = false local carFlyBodyVelocity, carFlyBodyGyro, carFlyMoveConnection = nil, nil, nil local carFlySpeed = 50 local function startCarFly() local char = LocalPlayer.Character local root = char and char:FindFirstChild("HumanoidRootPart") if not root then return end if carFlyBodyVelocity then carFlyBodyVelocity:Destroy() end if carFlyBodyGyro then carFlyBodyGyro:Destroy() end if carFlyMoveConnection then carFlyMoveConnection:Disconnect() end carFlyBodyVelocity = Instance.new("BodyVelocity", root) carFlyBodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge) carFlyBodyVelocity.Velocity = Vector3.new(0, 0, 0) carFlyBodyGyro = Instance.new("BodyGyro", root) carFlyBodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge) carFlyBodyGyro.D = 5000 carFlyBodyGyro.P = 50000 carFlyBodyGyro.CFrame = workspace.CurrentCamera.CFrame carFlyMoveConnection = RunService.RenderStepped:Connect(function() if not carFlyEnabled then return end local charNow = LocalPlayer.Character local rootNow = charNow and charNow:FindFirstChild("HumanoidRootPart") local humNow = charNow and charNow:FindFirstChildOfClass("Humanoid") if not rootNow or not humNow then return end local camCF = workspace.CurrentCamera.CFrame local direction = Vector3.zero if UserInputService:IsKeyDown(Enum.KeyCode.W) then direction = direction + camCF.LookVector end if UserInputService:IsKeyDown(Enum.KeyCode.S) then direction = direction - camCF.LookVector end if UserInputService:IsKeyDown(Enum.KeyCode.A) then direction = direction - camCF.RightVector end if UserInputService:IsKeyDown(Enum.KeyCode.D) then direction = direction + camCF.RightVector end if UserInputService:IsKeyDown(Enum.KeyCode.Space) then direction = direction + Vector3.new(0, 1, 0) end if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then direction = direction - Vector3.new(0, 1, 0) end if direction.Magnitude > 0 then carFlyBodyVelocity.Velocity = direction.Unit * carFlySpeed else carFlyBodyVelocity.Velocity = Vector3.zero end carFlyBodyGyro.CFrame = camCF end) end local function stopCarFly() if carFlyBodyVelocity then carFlyBodyVelocity:Destroy() carFlyBodyVelocity = nil end if carFlyBodyGyro then carFlyBodyGyro:Destroy() carFlyBodyGyro = nil end if carFlyMoveConnection then carFlyMoveConnection:Disconnect() carFlyMoveConnection = nil end local char = LocalPlayer.Character local root = char and char:FindFirstChild("HumanoidRootPart") if root then root.Anchored = false end end independentSection:Toggle({ Title = "飞车", Value = false, Callback = function(val) carFlyEnabled = val if val then startCarFly() else stopCarFly() end end }) local autoRebirthTask = nil independentSection:Toggle({ Title = "自动重生", Value = false, Callback = function(val) if val then if autoRebirthTask then task.cancel(autoRebirthTask) end autoRebirthTask = task.spawn(function() while true do pcall(function() local remotes = ReplicatedStorage:FindFirstChild("Remotes") or ReplicatedStorage for _, r in ipairs(remotes:GetDescendants()) do if r:IsA("RemoteEvent") and (r.Name:find("Rebirth") or r.Name:find("Prestige")) then r:FireServer() end end end) task.wait(1) end end) else if autoRebirthTask then task.cancel(autoRebirthTask) autoRebirthTask = nil end end end }) local autoGainRunning = false local autoGainConnection = nil local autoGainCharAddedCon = nil local function startAutoGain() local char = LocalPlayer.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") local hum = char:FindFirstChildOfClass("Humanoid") if not root or not hum then return end local parkSpot = root.Position local angle = 0 local radius = 3 local pace = 0.01 autoGainRunning = true autoGainConnection = RunService.Heartbeat:Connect(function(deltaTime) if not autoGainRunning then if autoGainConnection then autoGainConnection:Disconnect() autoGainConnection = nil end return end local char2 = LocalPlayer.Character if not char2 then autoGainRunning = false return end root = char2:FindFirstChild("HumanoidRootPart") hum = char2:FindFirstChildOfClass("Humanoid") if not root or not hum then return end angle = angle + (pace * deltaTime) / radius if angle > 2 * math.pi then angle = angle - 2 * math.pi end local px = parkSpot.X + radius * math.cos(angle) local pz = parkSpot.Z + radius * math.sin(angle) root.CFrame = CFrame.lookAt( Vector3.new(px, parkSpot.Y, pz), Vector3.new(px - math.sin(angle), parkSpot.Y, pz + math.cos(angle)) ) hum:Move(Vector3.new(-math.sin(angle), 0, math.cos(angle))) end) end local function stopAutoGain() autoGainRunning = false if autoGainConnection then autoGainConnection:Disconnect() autoGainConnection = nil end if autoGainCharAddedCon then autoGainCharAddedCon:Disconnect() autoGainCharAddedCon = nil end end independentSection:Toggle({ Title = "自动获取速度", Value = false, Callback = function(val) if val then startAutoGain() if autoGainCharAddedCon then autoGainCharAddedCon:Disconnect() end autoGainCharAddedCon = LocalPlayer.CharacterAdded:Connect(function() task.wait(0.5) if autoGainRunning then startAutoGain() end end) else stopAutoGain() end end }) local deleteEffectsConn = nil local disabledEffects = {} independentSection:Toggle({ Title = "删除所有特效", Value = false, Callback = function(val) if val then local function clean(obj) pcall(function() if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam") then if obj.Enabled then disabledEffects[obj] = true obj.Enabled = false end end if obj:IsA("Fire") or obj:IsA("Smoke") or obj:IsA("Explosion") or obj:IsA("Sparkles") then if obj.Enabled then disabledEffects[obj] = true obj.Enabled = false end end if obj:IsA("Light") or obj:IsA("PointLight") or obj:IsA("SpotLight") or obj:IsA("SurfaceLight") then if obj.Enabled then disabledEffects[obj] = true obj.Enabled = false end end if obj:IsA("BloomEffect") or obj:IsA("BlurEffect") or obj:IsA("ColorCorrectionEffect") or obj:IsA("SunRaysEffect") or obj:IsA("DepthOfFieldEffect") or obj:IsA("Atmosphere") then if obj.Enabled then disabledEffects[obj] = true obj.Enabled = false end end if obj:IsA("Highlight") or obj:IsA("SelectionBox") or obj:IsA("SelectionSphere") then if obj.Enabled then disabledEffects[obj] = true obj.Enabled = false end end if obj:IsA("Decal") then if obj.Enabled then disabledEffects[obj] = true obj.Enabled = false end end end) end for _, obj in pairs(game:GetDescendants()) do clean(obj) end deleteEffectsConn = game.DescendantAdded:Connect(clean) else for obj, _ in pairs(disabledEffects) do pcall(function() if obj and obj.Parent then obj.Enabled = true end end) end disabledEffects = {} if deleteEffectsConn then deleteEffectsConn:Disconnect() deleteEffectsConn = nil end end end }) independentSection:Button({ Title = "自动翻译", Callback = function() local HttpService = game:GetService("HttpService") local Players = game:GetService("Players") local RunService = game:GetService("RunService") local CoreGui = game:GetService("CoreGui") local LocalPlayer = Players.LocalPlayer local WindUI = loadstring(game:HttpGet("https://hygg520.github.io/1/main.txt"))() local Window = WindUI:CreateWindow({ Title = "自动翻译", Icon = "", IconThemed = true, Author = "", Folder = "自动翻译", Size = UDim2.fromOffset(520, 340), Transparent = true, Theme = "Dark", SideBarWidth = 0, ScrollBarEnabled = true, }) Window:EditOpenButton({ Title = "自动翻译", Icon = "", CornerRadius = UDim.new(0, 16), StrokeThickness = 2, Color = ColorSequence.new(Color3.fromHex("FF0F7B"), Color3.fromHex("F89B29")), Draggable = true, }) local translationEnabled = false local translationSpeed = 2 local translatedTexts = {} local originalTexts = {} local heartbeatConnection = nil local playerGuiConnection = nil local coreGuiConnection = nil local function translateText(text) if not text or text == "" or #text < 2 then return nil end if translatedTexts[text] then return translatedTexts[text] end local url = "https://translate.googleapis.com/translate_a/single?client=gtx&sl=auto&tl=zh-CN&dt=t&q=" .. HttpService:UrlEncode(text) local response = game:HttpGet(url) local decoded = HttpService:JSONDecode(response) if decoded and decoded[1] and decoded[1][1] and decoded[1][1][1] then local result = decoded[1][1][1] translatedTexts[text] = result return result end return nil end local function isEnglish(text) if not text or text == "" then return false end local englishCount = 0 local totalCount = 0 for char in text:gmatch(".") do local byte = string.byte(char) if byte then totalCount = totalCount + 1 if (byte >= 65 and byte <= 90) or (byte >= 97 and byte <= 122) then englishCount = englishCount + 1 end end end if totalCount == 0 then return false end return (englishCount / totalCount) > 0.5 end local function processTextObject(textObject) if not translationEnabled then return end if not textObject:IsA("TextLabel") and not textObject:IsA("TextButton") and not textObject:IsA("TextBox") then return end local originalText = textObject.Text if not originalText or originalText == "" then return end if not isEnglish(originalText) then return end local translatedText = translateText(originalText) if translatedText and translatedText ~= originalText then if not originalTexts[textObject] then originalTexts[textObject] = originalText end textObject.Text = translatedText end end local function scanAndTranslate(parent) if not translationEnabled then return end for _, descendant in pairs(parent:GetDescendants()) do if descendant:IsA("TextLabel") or descendant:IsA("TextButton") or descendant:IsA("TextBox") then task.spawn(function() processTextObject(descendant) end) end end end local function onDescendantAdded(descendant) if not translationEnabled then return end if descendant:IsA("TextLabel") or descendant:IsA("TextButton") or descendant:IsA("TextBox") then task.delay(0.1, function() processTextObject(descendant) end) end end local function startTranslation() if playerGuiConnection then playerGuiConnection:Disconnect() end if coreGuiConnection then coreGuiConnection:Disconnect() end if heartbeatConnection then heartbeatConnection:Disconnect() end local PlayerGui = LocalPlayer:WaitForChild("PlayerGui") playerGuiConnection = PlayerGui.DescendantAdded:Connect(onDescendantAdded) coreGuiConnection = CoreGui.DescendantAdded:Connect(onDescendantAdded) local lastScanTime = tick() local scanInterval = 1 / translationSpeed heartbeatConnection = RunService.Heartbeat:Connect(function() if not translationEnabled then return end local currentTime = tick() if currentTime - lastScanTime >= scanInterval then lastScanTime = currentTime task.spawn(function() scanAndTranslate(PlayerGui) end) for _, gui in pairs(CoreGui:GetChildren()) do if gui:IsA("ScreenGui") then task.spawn(function() scanAndTranslate(gui) end) end end end end) scanAndTranslate(PlayerGui) for _, gui in pairs(CoreGui:GetChildren()) do if gui:IsA("ScreenGui") then scanAndTranslate(gui) end end end local function stopTranslation() for textObject, originalText in pairs(originalTexts) do if textObject and textObject.Parent then textObject.Text = originalText end end originalTexts = {} if playerGuiConnection then playerGuiConnection:Disconnect() playerGuiConnection = nil end if coreGuiConnection then coreGuiConnection:Disconnect() coreGuiConnection = nil end if heartbeatConnection then heartbeatConnection:Disconnect() heartbeatConnection = nil end end local MainTab = Window:Tab({Title = "主要"}) MainTab:Paragraph({Title = "自动翻译", Desc = "请先加载此脚本开启翻译再加载你需要翻译的脚本\n特殊UI无法翻译"}) MainTab:Toggle({ Title = "自动翻译", Value = false, Callback = function(state) translationEnabled = state if translationEnabled then startTranslation() else stopTranslation() end end }) MainTab:Slider({ Title = "翻译速度", Value = {Min = 1, Max = 10, Default = 2}, Callback = function(value) translationSpeed = value end }) Window:SelectTab(1) end }) independentSection:Button({ Title = "去除危险品", Callback = function() if _G.RemoveDangers then return end _G.RemoveDangers = true local function isKillish(inst) local name = string.lower(inst.Name) local KILL_KEYWORDS = {"kill","damage","hurt","lava","spike","poison","acid","death","saw","blade"} for _, kw in ipairs(KILL_KEYWORDS) do if string.find(name, kw, 1, true) then return true end end for _, child in ipairs(inst:GetChildren()) do if child:IsA("LuaSourceContainer") then local cname = string.lower(child.Name) if string.find(cname, "kill", 1, true) or string.find(cname, "damage", 1, true) or string.find(cname, "hurt", 1, true) or string.find(cname, "death", 1, true) then return true end end end return false end local function isKillNPC(inst) if not inst:IsA("Model") then return false end if Players:GetPlayerFromCharacter(inst) then return false end if not inst:FindFirstChildOfClass("Humanoid") then return false end local name = string.lower(inst.Name) local NPC_KEYWORDS = {"killer","monster","zombie","npc","boss","guard","enemy","chase","horror","demon","evil"} for _, kw in ipairs(NPC_KEYWORDS) do if string.find(name, kw, 1, true) then return true end end return false end local function destroyKillTargets() local queue = {Workspace} local idx = 1 while idx <= #queue do if not _G.RemoveDangers then break end local node = queue[idx] idx = idx + 1 if node and node.Parent then local children = node:GetChildren() for _, child in ipairs(children) do if LocalPlayer.Character and child:IsDescendantOf(LocalPlayer.Character) then elseif isKillNPC(child) then child:Destroy() elseif isKillish(child) then child:Destroy() else if child:IsA("Model") or child:IsA("Folder") or not child:IsA("BasePart") then table.insert(queue, child) end end end end if idx % 200 == 0 then RunService.Heartbeat:Wait() end end end local conn = Workspace.DescendantAdded:Connect(function(inst) if not _G.RemoveDangers then return end if LocalPlayer.Character and inst:IsDescendantOf(LocalPlayer.Character) then return end if isKillNPC(inst) then inst:Destroy() end if isKillish(inst) then inst:Destroy() end end) task.spawn(destroyKillTargets) _G.RemoveDangersConn = conn end }) independentSection:Button({ Title = "去除所有NPC", Callback = function() if _G.RemoveAllNPC then return end _G.RemoveAllNPC = true local function isRemovableNPC(inst) if not inst:IsA("Model") then return false end if Players:GetPlayerFromCharacter(inst) then return false end if not inst:FindFirstChildOfClass("Humanoid") then return false end return true end local function destroyNPCs() local queue = {Workspace} local idx = 1 while idx <= #queue do if not _G.RemoveAllNPC then break end local node = queue[idx] idx = idx + 1 if node and node.Parent then local children = node:GetChildren() for _, child in ipairs(children) do if LocalPlayer.Character and child:IsDescendantOf(LocalPlayer.Character) then elseif isRemovableNPC(child) then child:Destroy() else table.insert(queue, child) end end end if idx % 150 == 0 then RunService.Heartbeat:Wait() end end end local conn = Workspace.DescendantAdded:Connect(function(inst) if not _G.RemoveAllNPC then return end if LocalPlayer.Character and inst:IsDescendantOf(LocalPlayer.Character) then return end if isRemovableNPC(inst) then inst:Destroy() end end) task.spawn(destroyNPCs) _G.RemoveAllNPCConn = conn end }) local isLyingDown = false local lieDownJumpConn = nil local function standUpFromLieDown() if not isLyingDown then return end isLyingDown = false local char = LocalPlayer.Character if char then local hum = char:FindFirstChildOfClass("Humanoid") if hum then hum.Sit = false hum:ChangeState(Enum.HumanoidStateType.GettingUp) end end if lieDownJumpConn then lieDownJumpConn:Disconnect() lieDownJumpConn = nil end end independentSection:Button({ Title = "布娃娃状态", Callback = function() if isLyingDown then standUpFromLieDown() return end local char = LocalPlayer.Character if not char then return end local hum = char:FindFirstChildWhichIsA("Humanoid") if not hum then return end if hum.SeatPart then hum.Sit = false task.wait(0.5) end isLyingDown = true hum.Sit = true task.wait(0.1) if hum.RootPart then local cf = hum.RootPart.CFrame hum.RootPart.CFrame = cf * CFrame.Angles(math.rad(90), 0, 0) end for _, v in ipairs(hum:GetPlayingAnimationTracks()) do v:Stop() end lieDownJumpConn = UserInputService.JumpRequest:Connect(function() standUpFromLieDown() end) end }) independentSection:Button({ Title = "自杀", Callback = function() local char = LocalPlayer.Character if char then local hum = char:FindFirstChildOfClass("Humanoid") if hum then hum.Health = 0 end end end }) tab:Divider() local visualSection = tab:Section({Title = "视觉"}) local espPlayerCons = {} local function addESP(character) if not character then return end local oldHL = character:FindFirstChild("NH_ESP") if oldHL then oldHL:Destroy() end local highlight = Instance.new("Highlight") highlight.Name = "NH_ESP" highlight.FillTransparency = 0.7 highlight.OutlineTransparency = 0 highlight.FillColor = Color3.fromRGB(255, 0, 0) highlight.OutlineColor = Color3.fromRGB(255, 255, 255) highlight.Adornee = character highlight.Parent = character end local function hookPlayer(player) if player == LocalPlayer then return end if player.Character then addESP(player.Character) end local con = player.CharacterAdded:Connect(function(char) task.wait(0.2) addESP(char) end) table.insert(espPlayerCons, con) end visualSection:Toggle({ Title = "透视玩家", Value = false, Callback = function(enabled) _G.ESPEnabled = enabled if enabled then for _, player in ipairs(Players:GetPlayers()) do hookPlayer(player) end _G.ESPAddedCon = Players.PlayerAdded:Connect(function(player) if _G.ESPEnabled then hookPlayer(player) end end) else if _G.ESPAddedCon then _G.ESPAddedCon:Disconnect() _G.ESPAddedCon = nil end for _, con in ipairs(espPlayerCons) do con:Disconnect() end espPlayerCons = {} for _, player in ipairs(Players:GetPlayers()) do if player.Character then for _, desc in pairs(player.Character:GetDescendants()) do if desc:IsA("Highlight") and desc.Name == "NH_ESP" then desc:Destroy() end end end end for _, hl in pairs(workspace:GetDescendants()) do if hl:IsA("Highlight") and hl.Name == "NH_ESP" then hl:Destroy() end end end end }) local xrayAddedConn = nil local function applyXray(obj) if obj:IsA("BasePart") and not obj.Parent:FindFirstChildWhichIsA("Humanoid") and not (obj.Parent and obj.Parent.Parent and obj.Parent.Parent:FindFirstChildWhichIsA("Humanoid")) then obj.LocalTransparencyModifier = 0.5 end end visualSection:Toggle({ Title = "透视物体", Value = false, Callback = function(val) if val then _G.XrayOn = true for _, v in pairs(workspace:GetDescendants()) do applyXray(v) end if xrayAddedConn then xrayAddedConn:Disconnect() end xrayAddedConn = workspace.DescendantAdded:Connect(function(obj) if _G.XrayOn then applyXray(obj) end end) else _G.XrayOn = false if xrayAddedConn then xrayAddedConn:Disconnect() xrayAddedConn = nil end for _, v in pairs(workspace:GetDescendants()) do if v:IsA("BasePart") then v.LocalTransparencyModifier = 0 end end end end }) local boxDrawings = {} local boxEnabled = false local boxRenderConn = nil local function createBoxDrawings(player) if boxDrawings[player] then return end local lines = {} for i = 1, 12 do lines[i] = Drawing.new("Line") lines[i].Visible = false lines[i].Thickness = 2 lines[i].Transparency = 1 end boxDrawings[player] = lines end local function removeBoxDrawings(player) local lines = boxDrawings[player] if not lines then return end for _, line in ipairs(lines) do line:Remove() end boxDrawings[player] = nil end local function getRainbowColor() local hue = (tick() * 0.5) % 1 return Color3.fromHSV(hue, 1, 1) end visualSection:Toggle({ Title = "显示3D方框", Value = false, Callback = function(val) boxEnabled = val if val then if boxRenderConn then boxRenderConn:Disconnect() end boxRenderConn = RunService.RenderStepped:Connect(function() if not boxEnabled then return end local color = getRainbowColor() for _, player in ipairs(Players:GetPlayers()) do if player ~= LocalPlayer then local character = player.Character local humanoid = character and character:FindFirstChildOfClass("Humanoid") local root = character and character:FindFirstChild("HumanoidRootPart") if character and humanoid and humanoid.Health > 0 and root then createBoxDrawings(player) local lines = boxDrawings[player] local cf = root.CFrame local size = Vector3.new(4, 6, 2) local half = size / 2 local corners = { cf * CFrame.new(-half.X, -half.Y, -half.Z), cf * CFrame.new( half.X, -half.Y, -half.Z), cf * CFrame.new( half.X, half.Y, -half.Z), cf * CFrame.new(-half.X, half.Y, -half.Z), cf * CFrame.new(-half.X, -half.Y, half.Z), cf * CFrame.new( half.X, -half.Y, half.Z), cf * CFrame.new( half.X, half.Y, half.Z), cf * CFrame.new(-half.X, half.Y, half.Z), } local positions = {} for i, corner in ipairs(corners) do positions[i] = corner.Position end local edges = { {1,2},{2,3},{3,4},{4,1}, {5,6},{6,7},{7,8},{8,5}, {1,5},{2,6},{3,7},{4,8}, } for i, edge in ipairs(edges) do local p1, p2 = positions[edge[1]], positions[edge[2]] local sp1, on1 = workspace.CurrentCamera:WorldToViewportPoint(p1) local sp2, on2 = workspace.CurrentCamera:WorldToViewportPoint(p2) if on1 and on2 then lines[i].From = Vector2.new(sp1.X, sp1.Y) lines[i].To = Vector2.new(sp2.X, sp2.Y) lines[i].Color = color lines[i].Visible = true else lines[i].Visible = false end end else if boxDrawings[player] then for _, line in ipairs(boxDrawings[player]) do line.Visible = false end end end end end end) else if boxRenderConn then boxRenderConn:Disconnect() boxRenderConn = nil end for player, lines in pairs(boxDrawings) do for _, line in ipairs(lines) do line:Remove() end end boxDrawings = {} end end }) local skeletonDrawings = {} local skeletonEnabled = false local skeletonRenderConn = nil local function getSkeletonPoints(character) local pts = {} pts.Head = character:FindFirstChild("Head") pts.UpperTorso = character:FindFirstChild("UpperTorso") or character:FindFirstChild("Torso") pts.LowerTorso = character:FindFirstChild("LowerTorso") pts.LeftUpperArm = character:FindFirstChild("LeftUpperArm") or character:FindFirstChild("Left Arm") pts.RightUpperArm = character:FindFirstChild("RightUpperArm") or character:FindFirstChild("Right Arm") pts.LeftLowerArm = character:FindFirstChild("LeftLowerArm") pts.RightLowerArm = character:FindFirstChild("RightLowerArm") pts.LeftHand = character:FindFirstChild("LeftHand") pts.RightHand = character:FindFirstChild("RightHand") pts.LeftUpperLeg = character:FindFirstChild("LeftUpperLeg") or character:FindFirstChild("Left Leg") pts.RightUpperLeg = character:FindFirstChild("RightUpperLeg") or character:FindFirstChild("Right Leg") pts.LeftLowerLeg = character:FindFirstChild("LeftLowerLeg") pts.RightLowerLeg = character:FindFirstChild("RightLowerLeg") pts.LeftFoot = character:FindFirstChild("LeftFoot") pts.RightFoot = character:FindFirstChild("RightFoot") return pts end local function createSkeletonDrawings(player) if skeletonDrawings[player] then return end local lines = {} for i = 1, 15 do lines[i] = Drawing.new("Line") lines[i].Visible = false lines[i].Thickness = 2 lines[i].Transparency = 1 end local headCircle = Drawing.new("Circle") headCircle.Visible = false headCircle.Thickness = 2 headCircle.NumSides = 20 headCircle.Radius = 5 headCircle.Filled = false headCircle.Transparency = 1 skeletonDrawings[player] = {lines = lines, headCircle = headCircle} end local function removeSkeletonDrawings(player) local d = skeletonDrawings[player] if not d then return end for _, line in ipairs(d.lines) do line:Remove() end d.headCircle:Remove() skeletonDrawings[player] = nil end local function setSkeletonLine(line, part1, part2, color) if not part1 or not part2 then line.Visible = false return end local p1, on1 = workspace.CurrentCamera:WorldToViewportPoint(part1.Position) local p2, on2 = workspace.CurrentCamera:WorldToViewportPoint(part2.Position) if on1 and on2 then line.From = Vector2.new(p1.X, p1.Y) line.To = Vector2.new(p2.X, p2.Y) line.Color = color line.Visible = true else line.Visible = false end end visualSection:Toggle({ Title = "显示骨骼", Value = false, Callback = function(val) skeletonEnabled = val if val then if skeletonRenderConn then skeletonRenderConn:Disconnect() end skeletonRenderConn = RunService.RenderStepped:Connect(function() if not skeletonEnabled then return end local color = getRainbowColor() for _, player in ipairs(Players:GetPlayers()) do if player ~= LocalPlayer then local character = player.Character local humanoid = character and character:FindFirstChildOfClass("Humanoid") if character and humanoid and humanoid.Health > 0 then createSkeletonDrawings(player) local d = skeletonDrawings[player] local pts = getSkeletonPoints(character) setSkeletonLine(d.lines[1], pts.Head, pts.UpperTorso, color) setSkeletonLine(d.lines[2], pts.UpperTorso, pts.LowerTorso, color) setSkeletonLine(d.lines[3], pts.UpperTorso, pts.LeftUpperArm, color) setSkeletonLine(d.lines[4], pts.UpperTorso, pts.RightUpperArm, color) setSkeletonLine(d.lines[5], pts.LeftUpperArm, pts.LeftLowerArm, color) setSkeletonLine(d.lines[6], pts.RightUpperArm, pts.RightLowerArm, color) setSkeletonLine(d.lines[7], pts.LeftLowerArm, pts.LeftHand, color) setSkeletonLine(d.lines[8], pts.RightLowerArm, pts.RightHand, color) setSkeletonLine(d.lines[9], pts.LowerTorso or pts.UpperTorso, pts.LeftUpperLeg, color) setSkeletonLine(d.lines[10], pts.LowerTorso or pts.UpperTorso, pts.RightUpperLeg, color) setSkeletonLine(d.lines[11], pts.LeftUpperLeg, pts.LeftLowerLeg, color) setSkeletonLine(d.lines[12], pts.RightUpperLeg, pts.RightLowerLeg, color) setSkeletonLine(d.lines[13], pts.LeftLowerLeg, pts.LeftFoot, color) setSkeletonLine(d.lines[14], pts.RightLowerLeg, pts.RightFoot, color) if pts.Head then local hp, on = workspace.CurrentCamera:WorldToViewportPoint(pts.Head.Position) if on then d.headCircle.Position = Vector2.new(hp.X, hp.Y) d.headCircle.Color = color d.headCircle.Visible = true else d.headCircle.Visible = false end else d.headCircle.Visible = false end else if skeletonDrawings[player] then for _, line in ipairs(skeletonDrawings[player].lines) do line.Visible = false end skeletonDrawings[player].headCircle.Visible = false end end end end end) else if skeletonRenderConn then skeletonRenderConn:Disconnect() skeletonRenderConn = nil end for player, _ in pairs(skeletonDrawings) do removeSkeletonDrawings(player) end skeletonDrawings = {} end end }) local healthGuis = {} local healthEnabled = false local healthRenderConn = nil local function createHealthGui(player) if healthGuis[player] then return end local gui = Instance.new("BillboardGui") gui.Name = "HealthESP_" .. player.Name gui.Size = UDim2.new(0, 100, 0, 8) gui.StudsOffset = Vector3.new(0, 2.8, 0) gui.AlwaysOnTop = true gui.Parent = workspace local bg = Instance.new("Frame") bg.Size = UDim2.new(1, 0, 1, 0) bg.BackgroundColor3 = Color3.new(0, 0, 0) bg.BorderSizePixel = 1 bg.BorderColor3 = Color3.new(1, 1, 1) bg.Parent = gui local bar = Instance.new("Frame") bar.Size = UDim2.new(1, 0, 1, 0) bar.BackgroundColor3 = Color3.new(0, 1, 0) bar.BorderSizePixel = 0 bar.Parent = bg healthGuis[player] = {gui = gui, bar = bar} end local function removeHealthGui(player) local d = healthGuis[player] if not d then return end d.gui:Destroy() healthGuis[player] = nil end visualSection:Toggle({ Title = "显示血量", Value = false, Callback = function(val) healthEnabled = val if val then if healthRenderConn then healthRenderConn:Disconnect() end healthRenderConn = RunService.RenderStepped:Connect(function() if not healthEnabled then return end for _, player in ipairs(Players:GetPlayers()) do if player ~= LocalPlayer then local character = player.Character local humanoid = character and character:FindFirstChildOfClass("Humanoid") local head = character and character:FindFirstChild("Head") if character and humanoid and humanoid.Health > 0 and head then createHealthGui(player) local d = healthGuis[player] d.gui.Adornee = head local ratio = humanoid.Health / humanoid.MaxHealth d.bar.Size = UDim2.new(ratio, 0, 1, 0) if ratio > 0.5 then d.bar.BackgroundColor3 = Color3.new(0, 1, 0) elseif ratio > 0.25 then d.bar.BackgroundColor3 = Color3.new(1, 1, 0) else d.bar.BackgroundColor3 = Color3.new(1, 0, 0) end else removeHealthGui(player) end end end end) else if healthRenderConn then healthRenderConn:Disconnect() healthRenderConn = nil end for player, _ in pairs(healthGuis) do removeHealthGui(player) end healthGuis = {} end end }) local NameTagEspFolder = Workspace:FindFirstChild("NameTagESP") or Instance.new("Folder") NameTagEspFolder.Name = "NameTagESP" NameTagEspFolder.Parent = Workspace local nameTagConnections = {} local function CleanUpPlayerNameTag(Player) if nameTagConnections[Player] then for _, Connection in ipairs(nameTagConnections[Player]) do Connection:Disconnect() end nameTagConnections[Player] = nil end local Container = NameTagEspFolder:FindFirstChild(Player.Name) if Container then Container:Destroy() end end local function CreateNameTagESP(Player) if Player == LocalPlayer then return end CleanUpPlayerNameTag(Player) nameTagConnections[Player] = {} local Container = Instance.new("Folder") Container.Name = Player.Name Container.Parent = NameTagEspFolder local function CreateNameTag(Char) if not Char then return end local Root = Char:WaitForChild("HumanoidRootPart", 5) if not Root then return end local OldTag = Container:FindFirstChild("EspNameTag") if OldTag then OldTag:Destroy() end local BbGui = Instance.new("BillboardGui") BbGui.Name = "EspNameTag" BbGui.AlwaysOnTop = true BbGui.Size = UDim2.new(0, 200, 0, 50) BbGui.StudsOffset = Vector3.new(0, 3, 0) BbGui.Adornee = Root BbGui.Parent = Container local TextLabel = Instance.new("TextLabel") TextLabel.Size = UDim2.new(1, 0, 1, 0) TextLabel.BackgroundTransparency = 1 TextLabel.Text = Player.Name TextLabel.TextColor3 = Color3.fromRGB(255, 0, 0) TextLabel.TextSize = 14 TextLabel.Font = Enum.Font.SourceSansBold TextLabel.TextStrokeTransparency = 0 TextLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0) TextLabel.Parent = BbGui end if Player.Character then CreateNameTag(Player.Character) end local CharAdded = Player.CharacterAdded:Connect(function(Char) task.wait(0.5) CreateNameTag(Char) end) table.insert(nameTagConnections[Player], CharAdded) end local nameTagPlayerAddedConn = nil visualSection:Toggle({ Title = "显示昵称", Value = false, Callback = function(val) if val then for _, Player in ipairs(Players:GetPlayers()) do CreateNameTagESP(Player) end if nameTagPlayerAddedConn then nameTagPlayerAddedConn:Disconnect() end nameTagPlayerAddedConn = Players.PlayerAdded:Connect(function(Player) CreateNameTagESP(Player) end) else if nameTagPlayerAddedConn then nameTagPlayerAddedConn:Disconnect() nameTagPlayerAddedConn = nil end for _, Player in ipairs(Players:GetPlayers()) do CleanUpPlayerNameTag(Player) end for _, child in ipairs(NameTagEspFolder:GetChildren()) do child:Destroy() end end end }) Players.PlayerRemoving:Connect(function(player) removeBoxDrawings(player) removeSkeletonDrawings(player) removeHealthGui(player) CleanUpPlayerNameTag(player) end) local lightSection = tab:Section({Title = "光影效果"}) local originalLighting2 = {} local originalTerrain = {} local createdEffects = {} local runningConnections = {} local function clearEffects() for _, effect in ipairs(createdEffects) do effect:Destroy() end createdEffects = {} for _, conn in ipairs(runningConnections) do conn:Disconnect() end runningConnections = {} end local function saveLighting() originalLighting2 = { Brightness = Lighting.Brightness, Ambient = Lighting.Ambient, OutdoorAmbient = Lighting.OutdoorAmbient, GlobalShadows = Lighting.GlobalShadows, ShadowSoftness = Lighting.ShadowSoftness, FogColor = Lighting.FogColor, FogEnd = Lighting.FogEnd, FogStart = Lighting.FogStart, ClockTime = Lighting.ClockTime, GeographicLatitude = Lighting.GeographicLatitude, ExposureCompensation = Lighting.ExposureCompensation, EnvironmentDiffuseScale = Lighting.EnvironmentDiffuseScale, EnvironmentSpecularScale = Lighting.EnvironmentSpecularScale } local terrain = Workspace:FindFirstChildOfClass("Terrain") if terrain then originalTerrain = { WaterReflectance = terrain.WaterReflectance, WaterTransparency = terrain.WaterTransparency, WaterWaveSize = terrain.WaterWaveSize, WaterWaveSpeed = terrain.WaterWaveSpeed, WaterColor = terrain.WaterColor } end end local function restoreLighting() for key, value in pairs(originalLighting2) do Lighting[key] = value end local terrain = Workspace:FindFirstChildOfClass("Terrain") if terrain then for key, value in pairs(originalTerrain) do terrain[key] = value end end end local function createEffect(effectType, properties) local effect = Instance.new(effectType) for prop, value in pairs(properties) do if effect[prop] ~= nil then effect[prop] = value end end effect.Parent = Lighting table.insert(createdEffects, effect) return effect end lightSection:Toggle({ Title = "真实光影", Value = false, Callback = function(val) if val then saveLighting() clearEffects() Lighting.Brightness = 0.2 Lighting.Ambient = Color3.new(0, 0, 0) Lighting.OutdoorAmbient = Color3.new(0, 0, 0) Lighting.GlobalShadows = false Lighting.EnvironmentDiffuseScale = 0 Lighting.EnvironmentSpecularScale = 0 else clearEffects() restoreLighting() end end }) lightSection:Toggle({ Title = "明亮光影", Value = false, Callback = function(val) if val then saveLighting() clearEffects() Lighting.ClockTime = 12 Lighting.GeographicLatitude = 0 Lighting.GlobalShadows = true Lighting.ShadowSoftness = 0.3 Lighting.Brightness = 3 Lighting.OutdoorAmbient = Color3.new(0.8, 0.8, 0.8) Lighting.Ambient = Color3.new(0.6, 0.6, 0.6) Lighting.Technology = Enum.Technology.ShadowMap createEffect("SunRaysEffect", {Intensity = 0.2, Spread = 0.5}) createEffect("BloomEffect", {Intensity = 0.3, Size = 10}) createEffect("Atmosphere", {Density = 0.1, Offset = 0.5, Color = Color3.new(0.9, 0.9, 0.9), Decay = Color3.new(0.9, 0.9, 0.9), Glare = 0, Haze = 0}) createEffect("ColorCorrectionEffect", {Saturation = -0.1, Contrast = 0.1}) createEffect("BlurEffect", {Size = 1}) else clearEffects() restoreLighting() end end }) lightSection:Toggle({ Title = "画质增强Pro Max", Value = false, Callback = function(val) if val then saveLighting() clearEffects() for _, child in ipairs(Lighting:GetChildren()) do if child:IsA("PostEffect") or child:IsA("Sky") then child:Destroy() end end createEffect("ColorCorrectionEffect", {Contrast = 0.18, Brightness = 0.05, Saturation = 0.3, TintColor = Color3.fromRGB(255, 242, 230)}) createEffect("BloomEffect", {Intensity = 0.25, Size = 48, Threshold = 0.85}) createEffect("SunRaysEffect", {Intensity = 0.25, Spread = 1.0}) createEffect("Atmosphere", {Density = 0.4, Offset = 0.25, Color = Color3.fromRGB(220, 220, 255), Decay = Color3.fromRGB(20, 25, 45), Glare = 0.15, Haze = 0.25}) local sky = Instance.new("Sky") sky.Parent = Lighting sky.SkyboxBk = "rbxassetid://6444337006" sky.SkyboxDn = "rbxassetid://6444336728" sky.SkyboxFt = "rbxassetid://6444337006" sky.SkyboxLf = "rbxassetid://6444337006" sky.SkyboxRt = "rbxassetid://6444337006" sky.SkyboxUp = "rbxassetid://6444336728" sky.StarCount = 3000 table.insert(createdEffects, sky) Lighting.GlobalShadows = true Lighting.ShadowSoftness = 0.05 Lighting.Brightness = 2.8 Lighting.ExposureCompensation = 0.7 Lighting.FogColor = Color3.fromRGB(140, 158, 178) Lighting.FogEnd = 3000 local terrain = Workspace:FindFirstChildOfClass("Terrain") if terrain then terrain.WaterReflectance = 0.35 terrain.WaterTransparency = 0.88 terrain.WaterWaveSize = 0.15 terrain.WaterWaveSpeed = 25 terrain.WaterColor = Color3.fromRGB(72, 141, 202) end else clearEffects() restoreLighting() end end }) lightSection:Toggle({ Title = "固定黄昏光影", Value = false, Callback = function(val) if val then saveLighting() clearEffects() for _, child in ipairs(Lighting:GetChildren()) do if child:IsA("PostEffect") or child:IsA("Atmosphere") then child:Destroy() end end Lighting.GlobalShadows = true Lighting.ShadowSoftness = 0.06 Lighting.Brightness = 3.5 Lighting.ExposureCompensation = 0.5 Lighting.EnvironmentSpecularScale = 1.2 Lighting.EnvironmentDiffuseScale = 0.5 Lighting.FogColor = Color3.fromRGB(130, 140, 160) Lighting.FogEnd = 8000 Lighting.FogStart = 50 Lighting.OutdoorAmbient = Color3.fromRGB(120, 130, 150) Lighting.GeographicLatitude = 40.0 Lighting.ClockTime = 16.5 createEffect("ColorCorrectionEffect", {Contrast = 0.15, Brightness = 0.04, Saturation = 0.25, TintColor = Color3.fromRGB(255, 245, 235)}) createEffect("BloomEffect", {Intensity = 0.08, Size = 40, Threshold = 0.95}) createEffect("SunRaysEffect", {Intensity = 0.22, Spread = 0.8}) createEffect("DepthOfFieldEffect", {FarIntensity = 0.1, FocusDistance = 35, InFocusRadius = 22, NearIntensity = 0.3}) createEffect("Atmosphere", {Density = 0.25, Offset = 0.25, Color = Color3.fromRGB(180, 190, 210), Decay = Color3.fromRGB(35, 40, 50), Haze = 0.12}) local terrain = Workspace:FindFirstChildOfClass("Terrain") if terrain then terrain.WaterReflectance = 0.2 terrain.WaterTransparency = 0.93 terrain.WaterWaveSize = 0.12 terrain.WaterWaveSpeed = 16 terrain.WaterColor = Color3.fromRGB(75, 135, 200) end local sky = Lighting:FindFirstChildOfClass("Sky") or Instance.new("Sky") sky.Parent = Lighting sky.CelestialBodiesShown = true sky.StarCount = 3500 sky.SkyboxBk = "rbxassetid://2711140172" sky.SkyboxDn = "rbxassetid://2711140372" sky.SkyboxFt = "rbxassetid://2711140592" sky.SkyboxLf = "rbxassetid://2711140792" sky.SkyboxRt = "rbxassetid://2711141042" sky.SkyboxUp = "rbxassetid://2711141282" table.insert(createdEffects, sky) Workspace.CurrentCamera.FieldOfView = 70 else clearEffects() restoreLighting() end end }) return tab end
