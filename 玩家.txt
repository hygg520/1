return function(Window, WindUI) local Players = game:GetService("Players") local RunService = game:GetService("RunService") local TweenService = game:GetService("TweenService") local Workspace = game:GetService("Workspace") local LocalPlayer = Players.LocalPlayer local tab = Window:Tab({Title = "玩家", Icon = "users"}) local targetPlayer = nil local playerMap = {} local function getPlayerNames() local names = {} playerMap = {} for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer then local label = p.DisplayName .. " (@" .. p.Name .. ")" table.insert(names, label) playerMap[label] = p end end return names end local playerSection = tab:Section({Title = "玩家选项", Box = true}) local playerDropdown = playerSection:Dropdown({ Title = "选择目标玩家", Values = getPlayerNames(), Callback = function(selected) if selected then targetPlayer = playerMap[selected] end end }) local function refreshDropdown() local newNames = getPlayerNames() if playerDropdown then if playerDropdown.SetValues then playerDropdown:SetValues(newNames) elseif playerDropdown.Update then playerDropdown:Update(newNames) elseif playerDropdown.UpdateValues then playerDropdown:UpdateValues(newNames) elseif playerDropdown.Refresh then playerDropdown:Refresh(newNames) else local oldCallback = playerDropdown.Callback if playerDropdown.Remove then playerDropdown:Remove() elseif playerDropdown.Destroy then playerDropdown:Destroy() end playerDropdown = playerSection:Dropdown({ Title = "选择目标玩家", Values = newNames, Callback = oldCallback or function(selected) if selected then targetPlayer = playerMap[selected] end end }) end else playerDropdown = playerSection:Dropdown({ Title = "选择目标玩家", Values = newNames, Callback = function(selected) if selected then targetPlayer = playerMap[selected] end end }) end end playerSection:Button({Title = "刷新玩家列表", Callback = function() refreshDropdown() WindUI:Notify({Title = "刷新成功", Content = "玩家列表已更新", Duration = 2, Icon = "check"}) end}) Players.PlayerAdded:Connect(refreshDropdown) Players.PlayerRemoving:Connect(refreshDropdown) local teleportSection = playerSection:Section({Title = "传送", Collapsible = true, Box = true}) teleportSection:Toggle({Title = "完全跟随", Value = false, Callback = function(val) if val then if not targetPlayer then WindUI:Notify({Title = "提示", Content = "请先选择目标玩家", Duration = 2, Icon = "alert"}) return end _G.FullFollowCon = RunService.Heartbeat:Connect(function() local myChar = LocalPlayer.Character if not myChar then return end local myRoot = myChar:FindFirstChild("HumanoidRootPart") if not myRoot then return end local target = targetPlayer if not target or not target.Character then return end local targetRoot = target.Character:FindFirstChild("HumanoidRootPart") if not targetRoot then return end myRoot.CFrame = targetRoot.CFrame end) else if _G.FullFollowCon then _G.FullFollowCon:Disconnect() _G.FullFollowCon = nil end end end}) teleportSection:Toggle({Title = "平滑跟随", Value = false, Callback = function(val) if val then if not targetPlayer then WindUI:Notify({Title = "提示", Content = "请先选择目标玩家", Duration = 2, Icon = "alert"}) return end _G.SmoothFollowCon = RunService.Heartbeat:Connect(function() local myChar = LocalPlayer.Character if not myChar then return end local myRoot = myChar:FindFirstChild("HumanoidRootPart") if not myRoot then return end local target = targetPlayer if not target or not target.Character then return end local targetRoot = target.Character:FindFirstChild("HumanoidRootPart") if not targetRoot then return end local offset = targetRoot.CFrame.LookVector * -2.5 myRoot.CFrame = CFrame.new(targetRoot.Position + offset, targetRoot.Position) end) else if _G.SmoothFollowCon then _G.SmoothFollowCon:Disconnect() _G.SmoothFollowCon = nil end end end}) teleportSection:Button({Title = "传送至选中玩家", Callback = function() if not targetPlayer then WindUI:Notify({Title = "提示", Content = "请先选择目标玩家", Duration = 2, Icon = "alert"}) return end local target = targetPlayer if not target.Character or not target.Character:FindFirstChild("HumanoidRootPart") then return end local myChar = LocalPlayer.Character if not myChar or not myChar:FindFirstChild("HumanoidRootPart") then return end local targetPos = target.Character.HumanoidRootPart.Position myChar.HumanoidRootPart.CFrame = CFrame.new(targetPos + Vector3.new(0, 0, 3)) end}) teleportSection:Button({Title = "传送最近玩家", Callback = function() local nearest = nil local minDist = math.huge local myChar = LocalPlayer.Character if not myChar then return end local myRoot = myChar:FindFirstChild("HumanoidRootPart") if not myRoot then return end local myPos = myRoot.Position for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer then local pChar = p.Character if pChar then local pRoot = pChar:FindFirstChild("HumanoidRootPart") if pRoot then local dist = (pRoot.Position - myPos).Magnitude if dist < minDist then minDist = dist nearest = p end end end end end if nearest then local targetRoot = nearest.Character and nearest.Character:FindFirstChild("HumanoidRootPart") if targetRoot and myRoot then myRoot.CFrame = targetRoot.CFrame + Vector3.new(0, 0, 3) WindUI:Notify({Title = "传送成功", Content = "已传送至 " .. nearest.DisplayName, Duration = 2, Icon = "check"}) end else WindUI:Notify({Title = "提示", Content = "未找到可传送的玩家", Duration = 2, Icon = "alert"}) end end}) local teleportAllRunning = false teleportSection:Toggle({Title = "传送所有人", Value = false, Callback = function(val) teleportAllRunning = val if val then task.spawn(function() local count = 0 while teleportAllRunning do local players = Players:GetPlayers() for _, p in ipairs(players) do if not teleportAllRunning then break end if p ~= LocalPlayer then local char = p.Character local myChar = LocalPlayer.Character if char and myChar then local targetRoot = char:FindFirstChild("HumanoidRootPart") local myRoot = myChar:FindFirstChild("HumanoidRootPart") if targetRoot and myRoot then local startCFrame = myRoot.CFrame local startTime = tick() local duration = 0.45 while tick() - startTime < duration do if not teleportAllRunning then break end local alpha = (tick() - startTime) / duration if alpha > 1 then alpha = 1 end local curTarget = char:FindFirstChild("HumanoidRootPart") if not curTarget then break end local goal = curTarget.CFrame myRoot.CFrame = startCFrame:Lerp(goal, alpha) RunService.Heartbeat:Wait() end local finalTarget = char:FindFirstChild("HumanoidRootPart") if finalTarget and myRoot then myRoot.CFrame = finalTarget.CFrame end count = count + 1 end end end end end if count > 0 then WindUI:Notify({Title = "传送所有人", Content = "已停止，共传送 " .. count .. " 次", Duration = 2, Icon = "check"}) else WindUI:Notify({Title = "传送所有人", Content = "已停止", Duration = 2, Icon = "alert"}) end end) end end}) playerSection:Divider() local trollingSection = playerSection:Section({Title = "恶搞", Collapsible = true, Box = true}) local suckConnection = nil local suckAnimation = nil local originalGravity = nil trollingSection:Toggle({Title = "口人", Value = false, Callback = function(val) if val then if not targetPlayer then WindUI:Notify({Title = "提示", Content = "请先选择目标玩家", Duration = 2, Icon = "alert"}) return end local target = targetPlayer local myChar = LocalPlayer.Character if not myChar then return end local myRoot = myChar:FindFirstChild("HumanoidRootPart") local targetChar = target.Character if not targetChar then return end local targetRoot = targetChar:FindFirstChild("HumanoidRootPart") if not myRoot or not targetRoot then return end originalGravity = Workspace.Gravity Workspace.Gravity = 0 local animation = Instance.new("Animation") animation.AnimationId = "rbxassetid://5918726674" local humanoid = myChar:FindFirstChildOfClass("Humanoid") if humanoid then suckAnimation = humanoid:LoadAnimation(animation) suckAnimation:Play() suckAnimation:AdjustSpeed(1) end local targetTorso = targetChar:FindFirstChild("LowerTorso") or targetChar:FindFirstChild("UpperTorso") if targetTorso then suckConnection = RunService.Heartbeat:Connect(function() if myRoot and targetTorso then myRoot.CFrame = targetTorso.CFrame * CFrame.new(0, -2.3, -1.0) * CFrame.Angles(0, math.pi, 0) end end) end else if suckConnection then suckConnection:Disconnect() suckConnection = nil end if suckAnimation then suckAnimation:Stop() suckAnimation = nil end if originalGravity then Workspace.Gravity = originalGravity originalGravity = nil end end end}) local bangConnection = nil local bangAnimation = nil trollingSection:Toggle({Title = "配人", Value = false, Callback = function(val) if val then if not targetPlayer then WindUI:Notify({Title = "提示", Content = "请先选择目标玩家", Duration = 2, Icon = "alert"}) return end local target = targetPlayer local myChar = LocalPlayer.Character if not myChar then return end local myRoot = myChar:FindFirstChild("HumanoidRootPart") if not myRoot then return end local animation = Instance.new("Animation") animation.AnimationId = "rbxassetid://10714068222" local humanoid = myChar:FindFirstChildOfClass("Humanoid") if humanoid then bangAnimation = humanoid:LoadAnimation(animation) bangAnimation:Play() bangAnimation:AdjustSpeed(2) end bangConnection = RunService.Heartbeat:Connect(function() if not val then return end local targetChar = target.Character if not targetChar then return end local targetHRP = targetChar:FindFirstChild("HumanoidRootPart") if not targetHRP or not myRoot then return end local forwardCFrame = targetHRP.CFrame * CFrame.new(0, 0, 1) local backwardCFrame = targetHRP.CFrame * CFrame.new(0, 0, 2.5) local tweenForward = TweenService:Create(myRoot, TweenInfo.new(0.2, Enum.EasingStyle.Linear, Enum.EasingDirection.Out), {CFrame = forwardCFrame}) tweenForward:Play() tweenForward.Completed:Wait() local tweenBackward = TweenService:Create(myRoot, TweenInfo.new(0.2, Enum.EasingStyle.Linear, Enum.EasingDirection.Out), {CFrame = backwardCFrame}) tweenBackward:Play() tweenBackward.Completed:Wait() end) else if bangConnection then bangConnection:Disconnect() bangConnection = nil end if bangAnimation then bangAnimation:Stop() bangAnimation = nil end end end}) local susConnection = nil local susAnimation = nil trollingSection:Toggle({Title = "被配", Value = false, Callback = function(val) if val then if not targetPlayer then WindUI:Notify({Title = "提示", Content = "请先选择目标玩家", Duration = 2, Icon = "alert"}) return end local target = targetPlayer local myChar = LocalPlayer.Character if not myChar then return end local myRoot = myChar:FindFirstChild("HumanoidRootPart") if not myRoot then return end local animation = Instance.new("Animation") animation.AnimationId = "rbxassetid://10714360343" local humanoid = myChar:FindFirstChildOfClass("Humanoid") if humanoid then susAnimation = humanoid:LoadAnimation(animation) susAnimation:Play() end susConnection = RunService.Heartbeat:Connect(function() if not val then return end local targetChar = target.Character if not targetChar then return end local targetHRP = targetChar:FindFirstChild("HumanoidRootPart") if not targetHRP or not myRoot then return end local forwardCFrame = targetHRP.CFrame * CFrame.new(0, 0, -1.5) local backwardCFrame = targetHRP.CFrame * CFrame.new(0, 0, -1.1) local tweenForward = TweenService:Create(myRoot, TweenInfo.new(0.15, Enum.EasingStyle.Linear, Enum.EasingDirection.Out), {CFrame = forwardCFrame}) tweenForward:Play() tweenForward.Completed:Wait() local tweenBackward = TweenService:Create(myRoot, TweenInfo.new(0.15, Enum.EasingStyle.Linear, Enum.EasingDirection.Out), {CFrame = backwardCFrame}) tweenBackward:Play() tweenBackward.Completed:Wait() end) else if susConnection then susConnection:Disconnect() susConnection = nil end if susAnimation then susAnimation:Stop() susAnimation = nil end end end}) local getSuckedConnection = nil local getSuckedAnimation = nil local getSuckedGravity = nil local getSuckedRunning = false trollingSection:Toggle({Title = "人口", Value = false, Callback = function(val) if val then if not targetPlayer then WindUI:Notify({Title = "提示", Content = "请先选择目标玩家", Duration = 2, Icon = "alert"}) return end getSuckedRunning = true local target = targetPlayer local myChar = LocalPlayer.Character if not myChar then return end local myRoot = myChar:FindFirstChild("HumanoidRootPart") local targetChar = target.Character if not targetChar then return end local targetRoot = targetChar:FindFirstChild("HumanoidRootPart") if not myRoot or not targetRoot then return end getSuckedGravity = Workspace.Gravity Workspace.Gravity = 0 coroutine.wrap(function() while getSuckedRunning and myRoot and targetRoot and myRoot.Position.Y <= 44 do task.wait() myRoot.CFrame = myRoot.CFrame * CFrame.new(0, 1.5, 0) end task.wait(1) if getSuckedRunning then local animation = Instance.new("Animation") animation.AnimationId = "rbxassetid://5918726674" local humanoid = myChar:FindFirstChildOfClass("Humanoid") if humanoid then getSuckedAnimation = humanoid:LoadAnimation(animation) getSuckedAnimation:Play() getSuckedAnimation:AdjustSpeed(1) end getSuckedConnection = RunService.Stepped:Connect(function() if getSuckedRunning and myRoot and targetRoot then myRoot.CFrame = targetRoot.CFrame * CFrame.new(0, 2.3, -1.1) * CFrame.Angles(0, math.pi, 0) myRoot.Velocity = Vector3.new(0, 0, 0) end end) end end)() else getSuckedRunning = false if getSuckedConnection then getSuckedConnection:Disconnect() getSuckedConnection = nil end if getSuckedAnimation then getSuckedAnimation:Stop() getSuckedAnimation = nil end if getSuckedGravity then Workspace.Gravity = getSuckedGravity getSuckedGravity = nil end end end}) local cameraFollowConnection = nil local isCameraFollowing = false trollingSection:Toggle({Title = "镜头跟随", Value = false, Callback = function(val) isCameraFollowing = val if val then if not targetPlayer then WindUI:Notify({Title = "提示", Content = "请先选择目标玩家", Duration = 2, Icon = "alert"}) return end local target = targetPlayer local targetChar = target.Character if not targetChar then WindUI:Notify({Title = "提示", Content = "目标玩家没有角色", Duration = 2, Icon = "alert"}) return end local targetHumanoid = targetChar:FindFirstChildOfClass("Humanoid") if not targetHumanoid then WindUI:Notify({Title = "提示", Content = "目标玩家没有Humanoid", Duration = 2, Icon = "alert"}) return end workspace.CurrentCamera.CameraSubject = targetHumanoid cameraFollowConnection = RunService.Heartbeat:Connect(function() if not isCameraFollowing then return end local currentTarget = targetPlayer if not currentTarget or not currentTarget.Character then return end local currentHumanoid = currentTarget.Character:FindFirstChildOfClass("Humanoid") if currentHumanoid then workspace.CurrentCamera.CameraSubject = currentHumanoid end end) WindUI:Notify({Title = "镜头跟随", Content = "已锁定 " .. target.DisplayName, Duration = 2, Icon = "check"}) else if cameraFollowConnection then cameraFollowConnection:Disconnect() cameraFollowConnection = nil end local myChar = LocalPlayer.Character if myChar then local myHumanoid = myChar:FindFirstChildOfClass("Humanoid") if myHumanoid then workspace.CurrentCamera.CameraSubject = myHumanoid end end WindUI:Notify({Title = "镜头跟随", Content = "已关闭", Duration = 2, Icon = "check"}) end end}) playerSection:Divider() local flingSection = playerSection:Section({Title = "甩飞", Collapsible = true, Box = true}) local function silentFling(targetPlayer) if not targetPlayer then return end local targetChar = targetPlayer.Character if not targetChar then return end local myChar = LocalPlayer.Character if not myChar then return end local myHumanoid = myChar:FindFirstChildOfClass("Humanoid") local myRoot = myHumanoid and myHumanoid.RootPart if not myRoot then return end local targetHumanoid = targetChar:FindFirstChildOfClass("Humanoid") local targetRoot = targetHumanoid and targetHumanoid.RootPart if not targetRoot then WindUI:Notify({Title = "甩飞失败", Content = "目标没有RootPart", Duration = 2, Icon = "alert"}) return end local originalSubject = workspace.CurrentCamera.CameraSubject local targetHumanoidForCam = targetChar:FindFirstChildOfClass("Humanoid") if targetHumanoidForCam then workspace.CurrentCamera.CameraSubject = targetHumanoidForCam end local oldPos = myRoot.CFrame local oldVel = myRoot.AssemblyLinearVelocity local oldRotVel = myRoot.AssemblyAngularVelocity myHumanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false) for _, otherPlayer in pairs(Players:GetPlayers()) do if otherPlayer ~= LocalPlayer then local otherChar = otherPlayer.Character if otherChar then for _, part in pairs(otherChar:GetDescendants()) do if part:IsA("BasePart") then pcall(function() part.CanCollide = false end) end end end end end local bv = Instance.new("BodyVelocity") bv.Name = "FlingVelocity" bv.Parent = myRoot bv.Velocity = Vector3.new(9e8, 9e8, 9e8) bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge) local startTime = tick() local direction = 1 local lastSwitch = tick() local detected = false while tick() - startTime < 2.5 do if not targetPlayer.Character then break end local currentTargetRoot = targetPlayer.Character:FindFirstChild("HumanoidRootPart") if not currentTargetRoot then break end local curTargetHum = targetPlayer.Character:FindFirstChildOfClass("Humanoid") if curTargetHum and workspace.CurrentCamera.CameraSubject ~= curTargetHum then workspace.CurrentCamera.CameraSubject = curTargetHum end if tick() - lastSwitch > 0.1 then direction = direction * -1 lastSwitch = tick() end local myChar2 = LocalPlayer.Character local myRoot2 = myChar2 and myChar2:FindFirstChild("HumanoidRootPart") if myRoot2 then local offset = direction * 2 local targetPos = currentTargetRoot.Position + currentTargetRoot.CFrame.LookVector * offset myRoot2.CFrame = CFrame.new(targetPos) myRoot2.AssemblyAngularVelocity = Vector3.new(50000, 50000, 50000) myRoot2.AssemblyLinearVelocity = Vector3.new(9e7, 9e7 * 10, 9e7) end task.wait(0.05) end bv:Destroy() myHumanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true) local myChar3 = LocalPlayer.Character local myRoot3 = myChar3 and myChar3:FindFirstChild("HumanoidRootPart") if myRoot3 then myRoot3.CFrame = oldPos myRoot3.AssemblyLinearVelocity = oldVel myRoot3.AssemblyAngularVelocity = oldRotVel end local myHumanoidNow = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") if myHumanoidNow then workspace.CurrentCamera.CameraSubject = myHumanoidNow elseif originalSubject then workspace.CurrentCamera.CameraSubject = originalSubject end task.wait(0.05) if myHumanoid then myHumanoid:ChangeState(Enum.HumanoidStateType.GettingUp) end end flingSection:Button({Title = "甩飞当前选中", Callback = function() if not targetPlayer then WindUI:Notify({Title = "提示", Content = "请先选择目标玩家", Duration = 2, Icon = "alert"}) return end local target = targetPlayer if not target or not target.Character then WindUI:Notify({Title = "提示", Content = "目标玩家不在游戏中", Duration = 2, Icon = "alert"}) return end local targetHum = target.Character:FindFirstChildOfClass("Humanoid") if not targetHum or targetHum.Health <= 0 then WindUI:Notify({Title = "提示", Content = "目标已死亡", Duration = 2, Icon = "alert"}) return end silentFling(target) WindUI:Notify({Title = "甩飞成功", Content = "已弹飞 " .. target.DisplayName, Duration = 1.5, Icon = "check"}) end}) local loopFlingRunning = false local loopFlingConnection = nil flingSection:Toggle({Title = "循环甩飞当前选中", Value = false, Callback = function(val) if val then if not targetPlayer then WindUI:Notify({Title = "提示", Content = "请先选择目标玩家", Duration = 2, Icon = "alert"}) return end loopFlingRunning = true local target = targetPlayer WindUI:Notify({Title = "循环甩飞", Content = "已开启，目标: " .. target.DisplayName, Duration = 2, Icon = "check"}) loopFlingConnection = RunService.Heartbeat:Connect(function() if not loopFlingRunning then loopFlingConnection:Disconnect() loopFlingConnection = nil return end if not targetPlayer or not targetPlayer.Character then return end local targetHum = targetPlayer.Character:FindFirstChildOfClass("Humanoid") if not targetHum or targetHum.Health <= 0 then return end silentFling(targetPlayer) task.wait(0.3) end) else loopFlingRunning = false if loopFlingConnection then loopFlingConnection:Disconnect() loopFlingConnection = nil end WindUI:Notify({Title = "循环甩飞", Content = "已关闭", Duration = 1.5, Icon = "check"}) end end}) flingSection:Button({Title = "甩飞所有玩家", Callback = function() local count = 0 for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer then local char = p.Character if char then local hum = char:FindFirstChildOfClass("Humanoid") if hum and hum.Health > 0 then silentFling(p) count = count + 1 task.wait(0.2) end end end end WindUI:Notify({Title = "甩飞所有玩家", Content = "已甩飞 " .. count .. " 名玩家", Duration = 2, Icon = "check"}) end}) local loopAllFlingRunning = false local loopAllFlingConnection = nil flingSection:Toggle({Title = "循环甩飞所有玩家", Value = false, Callback = function(val) if val then loopAllFlingRunning = true WindUI:Notify({Title = "循环甩飞所有玩家", Content = "已开启", Duration = 2, Icon = "check"}) loopAllFlingConnection = RunService.Heartbeat:Connect(function() if not loopAllFlingRunning then loopAllFlingConnection:Disconnect() loopAllFlingConnection = nil return end local players = Players:GetPlayers() for _, p in ipairs(players) do if not loopAllFlingRunning then break end if p ~= LocalPlayer then local char = p.Character if char then local hum = char:FindFirstChildOfClass("Humanoid") if hum and hum.Health > 0 then silentFling(p) task.wait(0.2) end end end end task.wait(0.3) end) else loopAllFlingRunning = false if loopAllFlingConnection then loopAllFlingConnection:Disconnect() loopAllFlingConnection = nil end WindUI:Notify({Title = "循环甩飞所有玩家", Content = "已关闭", Duration = 1.5, Icon = "check"}) end end}) return tab end
